// SowiMotor — Supabase connection config.
// Fill these in after creating your Supabase project (see SETUP.md), then
// save. Both public site and admin.html load this file.
//
// The anon key is meant to be public/client-side — it can only do what the
// row-level security policies in supabase-setup.sql allow (anyone can read
// bikes, only a logged-in user can add/edit/delete them).

window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
